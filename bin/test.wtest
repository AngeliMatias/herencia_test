import a.*
import b.*
import c.*

describe "a" {
	test "testA" {
		assert.equals(letraD.devolverLetraC(), "c")
	}
	test "testB" {
		assert.equals(letraD.devolverLetraB(), "b")
	}
	test "testD" {
		assert.equals(letraD.devolverLetraD(), "d")
	}
}
