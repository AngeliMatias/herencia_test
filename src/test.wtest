import a.*
import b.*

describe "a" {
	const objetoA = new LetterA()
	const objetoB = new LetterB()
	
	test "testA" {
		assert.equals(objetoA.devolverLetraA(), "a")
	}
	test "testB-A" {
		assert.equals(objetoB.devolverLetraA(), "a")
	}
	test "testB-B" {
		assert.equals(objetoB.devolverLetraB(), "b")
	}
}
